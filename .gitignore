# Compiled binaries
*.exe
/goaldl

# JetBrains IDE files
/.idea/

# Transient working captures and their derived outputs at repo root.
# Canonical test fixtures live in pkg/decoder/testdata/ instead.
/*.raw
/*.csv

# Saved grid tables from the TUI (s key): goaldl_<timestamp>_{BLM,INT,O2}.txt
/goaldl_*.txt

# GoReleaser build output
/dist/

# Downloaded community/vendor definition files (license/redistribution unverified —
# test fixtures must be written from scratch or cleared before committing)
/data/xdf/
