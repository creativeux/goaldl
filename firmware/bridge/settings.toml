# goaldl bridge configuration (CircuitPython reads this at boot)
# BRIDGE_MODE = "ap" creates its own WiFi network; "sta" joins an existing one.
BRIDGE_MODE = "ap"
BRIDGE_SSID = "goaldl"
BRIDGE_PASSWORD = "aldl1227"
BRIDGE_PORT = "3333"
# TEST mode: 1 = serve synthetic ALDL frames (no UART wiring needed); 0 = real UART.
BRIDGE_TEST = "1"
